import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header class="border-b h-8 flex justify-center items-center">
      <h1 class="text-xl font-bold mb-0.5">FAKA</h1>
    </header>
  );
});
